/**
 * Prefer the per-component subpath imports for better tree-shaking, e.g.:
 *   import { Button } from "@chill-institute/ui/components/ui/button";
 * This barrel exists for ergonomic re-exports only.
 */
export { cn } from "./lib/cn";
